# Configuración de despliegue en Netlify para Next.js (App Router) con pnpm.
[build]
  command = "pnpm build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
